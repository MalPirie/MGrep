<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
    ╔══════════════════════════════════════════════════════════════════════╗
    ║  APP-LEVEL CONTROL STYLES                                            ║
    ║                                                                      ║
    ║  All styles reference semantic tokens (BrushXxx) only.              ║
    ║  WPF-UI ControlsDictionary provides the base Fluent 2 templates;    ║
    ║  these styles extend or supplement them for app-specific needs.      ║
    ║                                                                      ║
    ║  RULES:                                                              ║
    ║    ✅  Use {DynamicResource BrushXxx} for all colour references.     ║
    ║    ✅  Use BasedOn to extend WPF-UI styles rather than replace them.  ║
    ║    ❌  No literal colour values.                                      ║
    ║    ❌  No MillimanXxx keys.                                           ║
    ║    ❌  No StaticResource for theme-sensitive brushes.                 ║
    ╚══════════════════════════════════════════════════════════════════════╝
    -->

    <!--
        CaptionButtonStyle — flat title-bar button for Minimize, Maximize/Restore,
        and the theme toggle. Transparent background; subtle hover; no border.
        WM_NCHITTEST (in MainWindow.xaml.cs) returns HTCLIENT for button regions
        so mouse events route normally.
    -->
    <Style x:Key="CaptionButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Width"         Value="46" />
        <Setter Property="Height"        Value="32" />
        <Setter Property="Background"    Value="Transparent" />
        <Setter Property="Foreground"    Value="{DynamicResource BrushForegroundPrimary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding"       Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <!-- x:Name lets triggers target the Border directly (more reliable than TemplateBinding) -->
                    <Border x:Name="Bd"
                            Background="Transparent"
                            BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource BrushCaptionButtonHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.75" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        CaptionCloseButtonStyle — same as CaptionButtonStyle but with the
        Windows-convention red hover background and white icon on hover.
    -->
    <Style x:Key="CaptionCloseButtonStyle" TargetType="{x:Type Button}"
           BasedOn="{StaticResource CaptionButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Bd"
                            Background="Transparent"
                            BorderThickness="0">
                        <ContentPresenter x:Name="Cp"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource BrushCaptionCloseHover}" />
                            <Setter TargetName="Cp" Property="TextElement.Foreground"
                                    Value="{DynamicResource BrushForegroundOnAccent}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        StatusBar — rounded bottom corners + themed background/text.
        CornerRadius matches Windows 11 window chrome rounding.
        Background is set here so it's removed from the view element.
    -->
    <Style TargetType="{x:Type StatusBar}">
        <Setter Property="Background"  Value="{DynamicResource BrushStatusBar}" />
        <Setter Property="Foreground"  Value="{DynamicResource BrushStatusBarText}" />
        <Setter Property="Padding"     Value="4,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type StatusBar}">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource CornerRadiusStatusBar}"
                            Padding="{TemplateBinding Padding}">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        StatusBarItem — ensure text colour follows the theme.
    -->
    <Style TargetType="{x:Type StatusBarItem}"
           BasedOn="{StaticResource {x:Type StatusBarItem}}">
        <Setter Property="Foreground" Value="{DynamicResource BrushStatusBarText}" />
    </Style>

</ResourceDictionary>
