<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
    ╔══════════════════════════════════════════════════════════════════════╗
    ║  DARK THEME — SEMANTIC TOKENS                                        ║
    ║                                                                      ║
    ║  Maps Milliman brand primitives (Brand.xaml) to UI semantic roles.  ║
    ║  Also overrides WPF-UI brush keys so WPF-UI control templates       ║
    ║  automatically render with Milliman brand colours.                   ║
    ║                                                                      ║
    ║  RULES:                                                              ║
    ║    ✅  Only MillimanXxx keys may be referenced here.                 ║
    ║    ✅  Controls reference BrushXxx keys via DynamicResource.         ║
    ║    ❌  No literal hex values.                                         ║
    ║    ❌  No DynamicResource needed here — Brand.xaml never changes.    ║
    ╚══════════════════════════════════════════════════════════════════════╝
    -->

    <!-- ── Application backgrounds ────────────────────────────────── -->
    <SolidColorBrush x:Key="BrushBackground"          Color="{StaticResource MillimanNeutral850}" />
    <SolidColorBrush x:Key="BrushSurface"             Color="{StaticResource MillimanNeutral800}" />
    <SolidColorBrush x:Key="BrushSurfaceSecondary"    Color="{StaticResource MillimanNeutral750}" />
    <SolidColorBrush x:Key="BrushSurfaceElevated"     Color="{StaticResource MillimanNeutral700}" />

    <!-- ── Foregrounds / text ──────────────────────────────────────── -->
    <SolidColorBrush x:Key="BrushForegroundPrimary"   Color="{StaticResource MillimanNeutral050}" />
    <SolidColorBrush x:Key="BrushForegroundSecondary" Color="{StaticResource MillimanNeutral400}" />
    <SolidColorBrush x:Key="BrushForegroundDisabled"  Color="{StaticResource MillimanNeutral600}" />
    <SolidColorBrush x:Key="BrushForegroundOnAccent"  Color="{StaticResource MillimanNeutral000}" />

    <!-- ── Accent (primary interactive colour) ─────────────────────── -->
    <SolidColorBrush x:Key="BrushAccent"              Color="{StaticResource MillimanBlue400}" />
    <SolidColorBrush x:Key="BrushAccentHover"         Color="{StaticResource MillimanBlue300}" />
    <SolidColorBrush x:Key="BrushAccentPressed"       Color="{StaticResource MillimanBlue500}" />
    <SolidColorBrush x:Key="BrushAccentDisabled"      Color="{StaticResource MillimanNeutral600}" />
    <SolidColorBrush x:Key="BrushAccentSubtle"        Color="{StaticResource MillimanBlue900}" />
    <SolidColorBrush x:Key="BrushAccentText"          Color="{StaticResource MillimanBlue300}" />

    <!-- ── Borders & focus ─────────────────────────────────────────── -->
    <SolidColorBrush x:Key="BrushBorder"              Color="{StaticResource MillimanNeutral700}" />
    <SolidColorBrush x:Key="BrushBorderStrong"        Color="{StaticResource MillimanNeutral600}" />
    <SolidColorBrush x:Key="BrushBorderFocus"         Color="{StaticResource MillimanBlue400}" />

    <!-- ── Status ──────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="BrushError"               Color="{StaticResource MillimanErrorRedDark}" />
    <SolidColorBrush x:Key="BrushWarning"             Color="{StaticResource MillimanWarningAmberDark}" />
    <SolidColorBrush x:Key="BrushSuccess"             Color="{StaticResource MillimanSuccessGreenDark}" />
    <SolidColorBrush x:Key="BrushInfo"                Color="{StaticResource MillimanInfoBlueDark}" />

    <!-- ── Shell chrome ────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="BrushStatusBar"           Color="{StaticResource MillimanNeutral950}" />
    <SolidColorBrush x:Key="BrushStatusBarText"       Color="{StaticResource MillimanNeutral300}" />

    <!--
    ════════════════════════════════════════════════════════════════════════
    WPF-UI BRUSH KEY OVERRIDES
    Override WPF-UI's internal tokens so that WPF-UI control templates
    automatically render with Milliman brand colours on dark backgrounds.
    ════════════════════════════════════════════════════════════════════════
    -->

    <!-- Text -->
    <SolidColorBrush x:Key="TextFillColorPrimaryBrush"          Color="{StaticResource MillimanNeutral050}" />
    <SolidColorBrush x:Key="TextFillColorSecondaryBrush"        Color="{StaticResource MillimanNeutral400}" />
    <SolidColorBrush x:Key="TextFillColorTertiaryBrush"         Color="{StaticResource MillimanNeutral500}" />
    <SolidColorBrush x:Key="TextFillColorDisabledBrush"         Color="{StaticResource MillimanNeutral600}" />
    <SolidColorBrush x:Key="TextFillColorInverseBrush"          Color="{StaticResource MillimanNeutral900}" />

    <!-- Accent text -->
    <SolidColorBrush x:Key="AccentTextFillColorPrimaryBrush"    Color="{StaticResource MillimanBlue300}" />
    <SolidColorBrush x:Key="AccentTextFillColorSecondaryBrush"  Color="{StaticResource MillimanBlue400}" />
    <SolidColorBrush x:Key="AccentTextFillColorTertiaryBrush"   Color="{StaticResource MillimanBlue200}" />
    <SolidColorBrush x:Key="AccentTextFillColorDisabledBrush"   Color="{StaticResource MillimanNeutral600}" />

    <!-- Text on accent background -->
    <SolidColorBrush x:Key="TextOnAccentFillColorPrimaryBrush"  Color="{StaticResource MillimanNeutral000}" />
    <SolidColorBrush x:Key="TextOnAccentFillColorSecondaryBrush" Color="{StaticResource MillimanNeutral050}" />
    <SolidColorBrush x:Key="TextOnAccentFillColorDisabledBrush" Color="{StaticResource MillimanNeutral700}" />
    <SolidColorBrush x:Key="TextOnAccentFillColorSelectedTextBrush" Color="{StaticResource MillimanNeutral000}" />

    <!-- Accent fills (buttons, toggles, progress bars) -->
    <SolidColorBrush x:Key="AccentFillColorDefaultBrush"        Color="{StaticResource MillimanBlue400}" />
    <SolidColorBrush x:Key="AccentFillColorSecondaryBrush"      Color="{StaticResource MillimanBlue300}" />
    <SolidColorBrush x:Key="AccentFillColorTertiaryBrush"       Color="{StaticResource MillimanBlue500}" />
    <SolidColorBrush x:Key="AccentFillColorDisabledBrush"       Color="{StaticResource MillimanNeutral700}" />
    <SolidColorBrush x:Key="AccentFillColorSelectedTextBackgroundBrush" Color="{StaticResource MillimanBlue400}" />

    <!-- Control fills (inputs, non-accent interactive surfaces) -->
    <SolidColorBrush x:Key="ControlFillColorDefaultBrush"       Color="{StaticResource MillimanNeutral800}" />
    <SolidColorBrush x:Key="ControlFillColorSecondaryBrush"     Color="{StaticResource MillimanNeutral750}" />
    <SolidColorBrush x:Key="ControlFillColorTertiaryBrush"      Color="{StaticResource MillimanNeutral700}" />
    <SolidColorBrush x:Key="ControlFillColorQuarternaryBrush"   Color="{StaticResource MillimanNeutral700}" />
    <SolidColorBrush x:Key="ControlFillColorDisabledBrush"      Color="{StaticResource MillimanNeutral850}" />
    <SolidColorBrush x:Key="ControlFillColorTransparentBrush"   Color="Transparent" />
    <SolidColorBrush x:Key="ControlFillColorInputActiveBrush"   Color="{StaticResource MillimanNeutral800}" />

    <!-- Control strokes (borders) -->
    <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush"     Color="{StaticResource MillimanNeutral700}" />
    <SolidColorBrush x:Key="ControlStrokeColorSecondaryBrush"   Color="{StaticResource MillimanNeutral600}" />
    <SolidColorBrush x:Key="ControlStrokeColorOnAccentDefaultBrush"   Color="{StaticResource MillimanBlue300}" />
    <SolidColorBrush x:Key="ControlStrokeColorOnAccentSecondaryBrush" Color="{StaticResource MillimanBlue200}" />
    <SolidColorBrush x:Key="ControlStrokeColorOnAccentTertiaryBrush"  Color="{StaticResource MillimanBlue500}" />
    <SolidColorBrush x:Key="ControlStrokeColorOnAccentDisabledBrush"  Color="{StaticResource MillimanNeutral700}" />

    <!-- Subtle fills (list item hover, row highlights) -->
    <SolidColorBrush x:Key="SubtleFillColorTransparentBrush"    Color="Transparent" />
    <SolidColorBrush x:Key="SubtleFillColorSecondaryBrush"      Color="{StaticResource MillimanNeutral750}" />
    <SolidColorBrush x:Key="SubtleFillColorTertiaryBrush"       Color="{StaticResource MillimanNeutral700}" />
    <SolidColorBrush x:Key="SubtleFillColorDisabledBrush"       Color="Transparent" />

    <!-- Layer fills (cards, panels) -->
    <SolidColorBrush x:Key="LayerFillColorDefaultBrush"         Color="{StaticResource MillimanNeutral800}" />
    <SolidColorBrush x:Key="LayerFillColorAltBrush"             Color="{StaticResource MillimanNeutral750}" />

    <!-- Application / window background -->
    <SolidColorBrush x:Key="ApplicationBackgroundBrush"         Color="{StaticResource MillimanNeutral850}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorBaseBrush"  Color="{StaticResource MillimanNeutral850}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorBaseAltBrush"      Color="{StaticResource MillimanNeutral800}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorSecondaryBrush"    Color="{StaticResource MillimanNeutral800}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorTertiaryBrush"     Color="{StaticResource MillimanNeutral750}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorQuarternaryBrush"  Color="{StaticResource MillimanNeutral700}" />

    <!-- Card backgrounds -->
    <SolidColorBrush x:Key="CardBackgroundFillColorDefaultBrush"   Color="{StaticResource MillimanNeutral800}" />
    <SolidColorBrush x:Key="CardBackgroundFillColorSecondaryBrush" Color="{StaticResource MillimanNeutral750}" />

    <!-- Focus -->
    <SolidColorBrush x:Key="FocusStrokeColorOuterBrush"         Color="{StaticResource MillimanBlue400}" />
    <SolidColorBrush x:Key="FocusStrokeColorInnerBrush"         Color="{StaticResource MillimanNeutral850}" />

    <!-- System status fills -->
    <SolidColorBrush x:Key="SystemFillColorSuccessBrush"        Color="{StaticResource MillimanSuccessGreenDark}" />
    <SolidColorBrush x:Key="SystemFillColorCautionBrush"        Color="{StaticResource MillimanWarningAmberDark}" />
    <SolidColorBrush x:Key="SystemFillColorCriticalBrush"       Color="{StaticResource MillimanErrorRedDark}" />
    <SolidColorBrush x:Key="SystemFillColorNeutralBrush"        Color="{StaticResource MillimanNeutral400}" />
    <SolidColorBrush x:Key="SystemFillColorSolidNeutralBrush"   Color="{StaticResource MillimanNeutral600}" />

</ResourceDictionary>
